<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="like.googlesupply">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".lifecycle.MainActivity">
        </activity>
        <activity android:name=".viewModel.Main2Activity" />
        <activity android:name=".liveData.Main3Activity" />
        <activity android:name=".room.Main4Activity" />
        <activity android:name=".allTest1.AllTestActivity" />
        <activity android:name=".transformations.TransformationsActivity" />
        <activity android:name=".myTest.MyTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>